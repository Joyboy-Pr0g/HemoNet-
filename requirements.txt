# Core (dataset_preparation.py, dataset_split.py)
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
optuna>=3.0
Pillow>=10.0

# PyTorch — default: wheels from PyPI (works for many setups).
# For CPU-only wheels from PyTorch (optional), add an extra index so PyPI still resolves pandas, etc.:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0
torchvision>=0.15
